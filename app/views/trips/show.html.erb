<p id="notice"><%= notice %></p>

<p>
  <strong>Driver:</strong>
  <%= @trip.driver_id %>
</p>

<p>
  <strong>Trip:</strong>
  <%= @trip.trip_id %>
</p>

<p>
  <strong>When:</strong>
  <%= @trip.when %>
</p>

<p>
  <strong>Destination:</strong>
  <%= @trip.destination %>
</p>

<p>
  <strong>Source:</strong>
  <%= @trip.source %>
</p>

<p>
  <strong>Petrolcost:</strong>
  <%= @trip.petrolcost %>
</p>

<p>
  <strong>Description:</strong>
  <%= @trip.description %>
</p>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
<%
  passengers = TripUser.where(TripID: @trip.trip_id)
%><br>There is currently
<%=  passengers.to_a.count %> passengers.

<% passengers.each do |trip_users| %>
  <%= trip_users.PassengerID %>
  <%  @lookup_user = User.where(email: user_id).includes(:brands).map { |article| [article.details, article.brands] }
  %>
  <%= @lookup_user_id.to_a.count %>
<% end %>

