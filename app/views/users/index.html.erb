<% @users = User.all %>
<% @users.each do |user| %>
  <%= user.email %><br/>
  <%=
    link_to "Delete",
            user_path(user),
            method: :delete,
            data: { confirm: "Are you sure you want to delete this user?" }
  %>
<% end %>