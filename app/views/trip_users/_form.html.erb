<%= form_with(model: trip_user, local: true) do |form| %>
  <% if trip_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_user.errors.count, "error") %> prohibited this trip_user from being saved:</h2>

      <ul>
      <% trip_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
